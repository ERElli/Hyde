{
  "name": "gamecode",
  "version": "1.0.0",
  "description": "All the up-to-date game code Consist of:   -index.html (file to start game in browser)   -/client/ (folder for all files run in the browser)   -/database/ (folder for all files to access the database)",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "mongojs": "^2.5.0",
    "socket.io": "^2.0.4"
  },
  "engines": {
    "node": "4.7.x"
  },
  "scripts": {
    "start": "node app.js"
  }
}
